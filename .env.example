# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/tamrenna_db"

# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d

# Client Configuration
VITE_API_URL=http://localhost:3001/api
VITE_APP_NAME="Tamrenna Gym Management"

# Email Configuration (for future use)
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# File Upload Configuration (for future use)
MAX_FILE_SIZE=5MB
UPLOAD_DIR=uploads